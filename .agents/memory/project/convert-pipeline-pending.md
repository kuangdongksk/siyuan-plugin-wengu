---
name: convert-pipeline-pending
description: 转换管线实测参数（5000字/批空行切块、检测窗口12000字N+下限）；生成默认1路串行=convertParallel从未设置、qid可解码逐题诞生时刻；串行/并发统一 agentChatOnce 都可选模型（旧通道区别已废）；题数语义=规则4按源类型分叉（试卷1:1/讲义造题）；0903 一题一答硬保证+重导入检测摘要弹窗已装机；0916 #131 断根真机核实生效、#148（真题聚合文档语篇材料块断裂+长选项替换截断）已派在途；待拍板（英语题型硬排除 steps、检测文案改「前段检测」、AI 切块已三理由否决待回应）
metadata:
    node_type: memory
    type: project
    originSessionId: sess_67681261-a222-4fcb-a95e-023806a604ea
---

2026-08-25 转换进度数字核查（用户问「15 批/检测共 6 题+/累计 8 题」对不对）得出的管线事实：

- **分批**：`chunkKramdown` 单批上限 5000 字（CHUNK_CHARS，比 MAX_SOURCE_CHARS 6000 小给 prompt 头留余量），在 [半长, 全长] 窗口找最后空行切块防拦腰切断公式/题目 → 48837 字符的控制系统第 1 章切出 15 批（平均 ~3300 字/批）。
- **前置检测**：`detectQuestions` 只看源文档**前 12000 字符**（DETECT_CHARS，防内核 AI 超时），AI 数可见部分现成题数；超窗时 `truncated=true`，count 是下限（界面显示「N+」）。「累计 8 > 检测 6」不矛盾——检测只覆盖前 1/4。
- **实测规模基准（2026-09-02，概率论与数理统计-题解 `20260828150255-64batvp`，162k 字符/3174 行）**：结构切块共 159 批——**每道习题的题干段与 `#### 答案` 段是两个独立块**（会话时间线交替 习题N/答案）；前置检测分段并行计数（该文档 10 段，面板见 检测 1/10~~10/10）；**串行（并发=1）约 1 分钟/批、全程 2.5h+**——嫌慢引导用户开转换弹窗「并发批次数」（1~~4，受供应商限流约束）。9 个纯标题空批的定诊见 [[convert-return-protocol-plan]]。
- **两个已提议、用户未拍板的修法**（下次继续时先问）：
    1. 「检测共 N 题+」文案误导（「共」读起来像全文总数）→ 截断时改「前段检测 N+ 题」，非截断维持「检测共 N 题」（改 ConvertDialog totalHint 分支 + i18n convertDetected）。
    2. prompt 规则 13「大题拆多步」只软限定工科大题，**没有硬排除英语题型**（cloze/match/essay/trans）用 steps → 建议规则 13 补硬排除一句（ConvertService buildPrompt）。
    3. **原位渐进展示的载体**（2026-08-26 已拍板落地，不再重提）：临时文档方案——原文档旁临时《·习题》渐进流式出题，终态替换原文档后删除；用户质疑过「数据两份」，但「做题渲染管线只吃真实文档 + 原文档中途不能动」两硬约束解释后接受。备选内存预览渲染（无文档、需并行渲染管线）已否。**注意：当初「内核无追加通道、每批只能删旧重建」的前提当天已被八轮真机探针推翻**（见下条已落地）。
- 工作区测试文档：控制系统第1章源文档 `20260811150547-tyblfbd`（MinerU 导入，48837 字符，题N标记约 28 个）；英语样卷习题 `20260824001029-40v0ou7`（E0 手造夹具，7 超级块 = trans/essay/cloze/2×single/2×材料块，**实测无 steps 题**）；知识点文档 `20260821143522-kaygs2d`。
- **已落地（2026-08-25，不再重提）**：渐进预览的 `isStarted()` 静默跳过已改为先收卷再接管；`setBusy` 漏禁 targetSel/targetInput 已补；题目索引轮询 10s→30s；切渐进文档清 finished/activeQIdx 残留；**kramdown 噪声双层清洗**——根因：转换输入是 `getBlockKramdown` 原始 kramdown（满屏 `{: id=…}` IAL 行 + `{{{row` 定界），AI 写解析时连标记抄回成裸文本；修法=输入侧（ConvertBatch）喂 AI 前剥块 id IAL 行（含 `> ` 引用前缀变体）+ 输出侧（ConvertService extractQuestions 规整链）剥含 `id=` 的 IAL 行（custom-plugin-wengu- 的保留）与嵌套 `{{{row`/`}}}`（容器自身首尾保留）。**只对新转换生效**：已生成的控制系统卷里的裸文本是旧数据，要干净须重转。
- **已落地（2026-08-26 补：转换流程页化）**：批次执行从弹窗闭包拆到单例运行器 ConvertRun.ts；弹窗（ConvertDialog）只收集参数，点开始即关；进度/停止/保留进度/全部丢弃改为页内转换条（ConvertHost renderConvertBar/replayConvertBar，lastBar 快照跨页签重渲染重放，终态 clear）；重复启动返回 false 提示「已有转换在进行中」；PDF 导入（MinerU）流程不变仍走弹窗内自有 stop。弹窗内的旧「终止二选一按钮/弹窗预览题行（dlg-preview 已删）」不复存在，别再按旧结构找。
- **诊断要点**：任一题卡片绑定抛异常会让**整页**「加载失败」（renderList try/catch 兜底）——2026-08-25 实例：AnswerFlow 选择器手误 `"[data-wordcount']"`（多一撇号）导致所有含作文题文档整页挂；这类字符串内容错误 tsc/eslint 查不出，遇到莫名加载失败先 grep querySelector 的选择器拼写。

**20260903 高数长跑收尾（用户已关闭思源，僵尸运行随之终结）**：

- **题目条数 ≠ 习题编号**（用户见「插件 180 道题」误以为全卷完成）：题干与答案解析各自成题，实测 **≈1.7 条/习题**（高数-题解 182 条 ↔ 覆盖习题1~107）；全卷 180 习题预计 **~310 条**。179 条撞上原文 180 习题纯属巧合，看进度要对 srcKey 覆盖面不要对条数。
- ~~rec 非空会挡住增量分支~~ **已代码化修复（20260903）**：reimportDocFrom 改为**带指纹即增量**（优先于 convertProgress 断点，陈旧 rec 自动清掉防「继续生成」重复生成）——不再需要手工清 rec。
- **0903 下午 重新导入「全被判例题跳过」定诊+修复（已装机 14:42 待用户验证）**：AI 会话登记簿抓到现行——增量补生成 29 块里 24 块 `CAN_CONVERT: no`「例题示范解答，跳过」。根因两层：① 题解书每道习题的 `#### 答案` 段独立成块，块里只有【解】没有题干，prompt 例题筛选「附完整示范解答即例题」正好误伤（题解书的核心内容恰是解答）；② 省费模式 convertKeepOld=true 把 IncrementDialog 整个跳过静默开跑，用户看不到任何检测环节。修复：prompt 加例外（习题册答案/解答区照转，题干由解答还原）；检测必过目——重导入一律先弹摘要（源共/已入库/待处理块数），省费模式只出摘要不出逐块清单；纯标题块入口前置滤除（无指纹会永远重算新增）；零产物块计数 IncrementOutcome.empty 终态点明（跳过的块无指纹，每次重导重发 AI——这是已知留白，别加「跳过指纹」黑名单，会把误杀块永久拉黑断绝重跑修复路）。
- 14:10 那轮无头补生成已被 petal 重载终止（生命周期闸），已入库块带指纹自愈；重开页签后题集右键「重新导入」应见检测摘要弹窗（源 ~360 块、已入库 132 块、待处理=剩余），答案块这轮应正常出题。
- **0903 傍晚 一题一答硬保证（用户定调「一定要保证试题跟原文一样」，已装机 15:0x 待验证）**：题数膨胀根因=题干块与答案块各转一题（1.7 条/习题）。修死两线：① structuralChunks 把答案类子节（键末段=答案/解答/题解/参考答案/答案与解析，~n 放行）**并入父题块**——真实机 369 块→189 块（正好 180 道习题），章级答案区父链对不上不并（保守）；② prompt 规则 4 改「一题对一题」——答案写进该题 @@P sol，不得拆多/合并/新造/为解答另出一题；讲义（无现成题目）才走造题分支。**存量指纹迁移走弹窗**：题干键 hash 全变→「变更」（默认保留旧题！要 1:1 须选重生成）、答案键消失→「消失」（选删除）——首次重导务必关省费模式逐块选，否则 keepOld 会把膨胀旧题留着只补新增。§ 见 SrcChunk.test.ts 17 例（新增并块 3 例）。
- **0903 用户提议「让 AI 切块」——已评估否决（待用户回应，用户不坚持就不翻案）**：用户觉得「切块很简单，交给 AI 应该没问题」。三理由否决：① 增量重导入的 srcKey/srcHash 指纹、三态分类、中断自愈全靠纯函数对同源文切出**逐字节相同**的结果，AI 切两次不一样→指纹全变、每次重导全判「变更」，哈希检测与省费模式报废；② 成本倒挂——现切块零 AI 调用微秒级，AI 切 24 万字文档要十几次额外调用且每次转换/重导重切；③ 切错是静默的——块边界错=题目悄悄漏/重，一题一答保证破（确定性切块有 17 单测钉着，AI 分段错要到题数不对才暴露）。规则盖不住的形态正道：会**看得见地**失败（答案块独立列在检测弹窗清单里用户自决），真遇到往 ANSWER_SECTION_RE 词表加一词（一行改动+一条单测），不换 AI。

**20260914 晚 用户挑战 A: 前缀整卷重转（「不管是不是批导入的都应该有哈希啊」）**：定诊——20260910 起所有转换（**含分片并行批量**：分片只给并行度，片内批边界仍由 AI `@@TO` 决定）产物 src-key 全是 `A:<偏移>`，`DocOps.ts:144` 见前缀即跳过三态分类整卷重转（notifyReimportCursor）；哈希本身不缺（questionHash/srcHash 都在），缺的是**可复现分块键**——源一改偏移全漂移，旧记录无法对位回源区域，与 0903 否决 AI 切块同一条确定性理由。已提两案待拍板：**方案 A（推荐）**=题集加「源内容哈希」optional 字段，重新导入先比对、源未变更零动作提示、变更才整卷重转；**方案 B**=重导时把 A: 偏移映射进确定性结构块做包含判定按块三态（贵、匹配易错，不建议先做）。用户点头即按 A 开单派青简。

相关：[[pivot-to-ai-block-conversion]]、[[siyuan-api-patterns]]、[[ui-consistency-feedback]]。
