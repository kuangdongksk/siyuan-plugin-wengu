/**
 * AI 调用超时档位（毫秒）——全仓统一收口（2026-08-27 起替代各域散落
 * 的 *_TIMEOUT_MS 常量）。按场景输出长度选档，不要在调用点自造数字：
 *  - react  看板娘表情反应（输出一行）
 *  - chat   看板娘聊天
 *  - quick  判分/引导/检测/统计/轮次报告/知识点路由（短输出）
 *  - mid    单词复盘批、变式自检验算
 *  - long   出题/重新生成/串行转换主批次
 *  - batch  并发转换池（长批次）
 * 全部通道走 agentChatOnce（SSE 流式），超时按**空闲**计——每收到一段
 * 流数据即续期，这里的值是空闲上限。
 */
export const AI_TIMEOUT = {
    react: 30_000,
    chat: 90_000,
    quick: 120_000,
    mid: 180_000,
    long: 300_000,
    batch: 600_000,
} as const;
