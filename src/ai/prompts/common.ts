/**
 * 全仓 AI prompt 的公共片段。20260910 起 prompt 集中收口进 src/ai/prompts/
 * （此前 26 处构建点散在 10 个业务文件里手抄重复）。
 *
 * 只收**逐字相同**的多处共用文本——插值拼出与原分叉版本逐字节一致的
 * 字符串（第一笔纯搬迁零行为变化）；措辞相近但不一致的不强凑，留在
 * 各自 builder 里保持原样。
 */

/** 错因六选一枚举（brief 判分 CAUSE 行与客观错题批量归因共用，
 *  WeaknessStore.normalizeCause 的取值域）。 */
export const CAUSE_LIST = "概念不清/计算失误/方法选择错/公式记错/审题失误/其他";

/** 单题生成的输出约束句（出题概念/变式/单题重生成三处共用）。 */
export const SINGLE_Q_NOTE = "只输出一道题的行协议（格式如下），格式之外不要输出任何文字。";
