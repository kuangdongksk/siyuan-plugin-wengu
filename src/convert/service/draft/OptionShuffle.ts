/**
 * 位置敏感措辞（选项组跳过重排的唯一口径）。
 *
 * 「以上都对」「A 和 B」一类选项一旦重排就指代错乱，故凡消费本判据的
 * 重排动作一律**保留 AI 原序**。两份活消费方：
 *   - `bank/data/BankRepair`（存量挤行修复的洗牌）
 *   - `quiz/render/CardDisplayShuffle`（进卡现洗的展示层）
 * 改判据只改这里，别在调用侧另起正则。
 *
 * ⚠️ **本模块自 Issue #214（20260922）起只剩这一个导出**：原 AI 生成选
 * 择题的选项洗牌（`shuffleDraftOptions` 入口、`shuffleGroup`/`collectGroups`
 * 洗牌实现、`unpackPackedSingle` 挤行拆行）与整份专用单测随存量兼容口径
 * 一并删除——那些入口自 Issue #131（20260915）起在生成链上已整体闲置
 * （库与题源文档统一「死形态」：选项按原文顺序、答案字母指向原文位置），
 * 而它们服务的存量数据经仓库主人确认不再兼容。**两个活后代在别处**：
 * 挤行拆行在 `draft/OptionUnpack.unpackPackedOptions`（三条落库链消费），
 * 判据就是本文件。
 * 死形态下若在生成链上恢复洗牌 = 把死形态又洗乱，别再接线。
 * 文件名叫 `OptionShuffle` 是历史遗留（判据当年就住这里），改名的收益
 * 不抵两处活 import 的动面。
 */

/** 位置敏感措辞：洗牌会破坏指代关系，保留 AI 原序。 */
export const POSITION_SENSITIVE = /(以上|上述|都不|都是|全都|全部|均正确|均错误|\b[A-D]\b\s*(?:和|与|及))/;
