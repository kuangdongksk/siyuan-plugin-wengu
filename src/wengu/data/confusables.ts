// 由 tools/gen-confusables.mjs 生成，勿手改：预置易混组（docs/confusable-words.md）。
// B 案：工具就位后手动跑一次生成替换本文件；当前为空占位，实证/AI 组在插件数据里。
const groups: {ids: number[]; src: "preset" | "ai" | "evidence"; raw?: string;}[] = [];

export default groups;
